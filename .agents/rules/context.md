---
trigger: always_on
---

1. after every request update a "PROJECT_CONTEXT.md" file in the project directory so that the nect agent knows what is going on with the project till now so that even if it looses conversation history it know where to start from.
keep on adding context untill the user specifically says to reset it.