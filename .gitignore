# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directories
node_modules/
jspm_packages/
.next/
dist/

# Env files (NEVER COMMIT SECRETS)
.env
.env.*
!.env.example
!.env.*.example

# Editor / OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Next.js build output
/web/.next/
/web/out/
/web/build/
web/next-env.d.ts
*.tsbuildinfo

# Scanner build output
/scanner/dist/

# Prisma
*.db
*.db-journal

# Backups / stray edit files
*.bak

# Local runtime data created by the installer / a local deployment
/data/
/backups/

# Deployment-specific infrastructure notes (contains real hostnames, IPs,
# tunnel IDs, etc. for whichever machine this is deployed on — never shared)
VPS_CONTEXT.md

# Agent tooling artifacts — local to a given editor/assistant setup, kept on
# disk for continuity across chat sessions but never published
.agents/
.claude/
CLAUDE.md
CLAUDE.local.md
